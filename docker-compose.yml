services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - hf-cache:/root/.cache/huggingface
    environment:
      - DEVICE=cpu
      - INPAINT_STEPS=8
      - GUIDANCE_SCALE=7.5

volumes:
  hf-cache:
